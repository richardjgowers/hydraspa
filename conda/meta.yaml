package:
  name: hydraspa
  version: 0.0.5

source:
  path: ../

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - docopt
    - numpy
    - pandas
    - scipy
    - statsmodels
    - mdanalysis

test:
  imports:
    - hydraspa